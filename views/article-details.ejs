<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title><%= article.title %></title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f9f9f9;
      padding: 30px;
      direction: rtl;
      text-align: right;
    }

    h1 {
      color: #222;
      margin-bottom: 10px;
    }

    p {
      font-size: 18px;
      line-height: 1.6;
      margin-bottom: 20px;
    }

    strong {
      color: #555;
    }

    .actions {
      margin-top: 30px;
    }

    .btn {
      display: inline-block;
      padding: 10px 15px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: bold;
      font-size: 16px;
      margin-left: 10px;
    }

    .btn-delete {
      background-color: #dc3545;
      color: white;
      border: none;
      cursor: pointer;
    }

    .btn-delete:hover {
      background-color: #c82333;
    }

    .btn-back {
      background-color: #007BFF;
      color: white;
    }

    .btn-back:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

  <h1><%= article.title %></h1>
  <p><%= article.body %></p>
  <p><strong>الكاتب:</strong> <%= article.author %></p>
<div class="actions" style="display: inline;">
<form action="/article/<%= article._id %>/like" method="POST" style="display:inline;">
  <button type="submit" style="background: none; border: none; cursor: pointer;">
    <% if (user && article.likedBy.includes(user.emails[0].value)) { %>
      
      <svg xmlns="http://www.w3.org/2000/svg" fill="red" width="28" height="28" viewBox="0 0 24 24">
        <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 
                 2 5.42 4.42 3 7.5 3c1.74 0 3.41 0.81 
                 4.5 2.09C13.09 3.81 14.76 3 16.5 3 
                 19.58 3 22 5.42 22 8.5c0 3.78-3.4 
                 6.86-8.55 11.54L12 21.35z"/>
      </svg>
    <% } else { %>
     
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" stroke="gray" stroke-width="2" width="28" height="28" viewBox="0 0 24 24">
        <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 
                 2 5.42 4.42 3 7.5 3c1.74 0 3.41 0.81 
                 4.5 2.09C13.09 3.81 14.76 3 16.5 3 
                 19.58 3 22 5.42 22 8.5c0 3.78-3.4 
                 6.86-8.55 11.54L12 21.35z"/>
      </svg>
    <% } %>
    <span style="font-size: 16px; color: #555;"><%= article.likes %></span>
  </button>
</form>


  <% if (user && user.emails[0].value === article.author) { %>
    <form action="/article/<%= article._id %>?_method=DELETE" method="POST" style="display: inline;">
      <button type="submit" class="btn btn-delete">🗑 حذف المقال</button>
    </form>
  <% } %>

  <a href="/show" class="btn btn-back">🔙 العودة للمقالات</a>
</div>

</body>
</html>
