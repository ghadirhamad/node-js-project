<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>كل المقالات</title>
  <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Tajawal', sans-serif;
      background: linear-gradient(to right, #f0f2f5, #d9e4f5);
      padding: 20px;
      direction: rtl;
      text-align: right;
    }

    h1 {
      margin-bottom: 20px;
      color: #333;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      background-color: white;
      padding: 15px;
      margin-bottom: 12px;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      transition: background 0.3s;
    }

    li:hover {
      background-color: #eef1f4;
    }

    a {
      text-decoration: none;
      color: #007BFF;
      font-weight: bold;
    }

    a:hover {
      color: #0056b3;
    }

    .nav {
      margin-top: 30px;
    }

    .nav a {
      margin-left: 10px;
      padding: 10px 18px;
      border-radius: 8px;
      background-color: #007BFF;
      color: white;
      text-decoration: none;
    }

    .nav a:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <%- include("partials/header") %>

  <h1>📚 قائمة المقالات</h1>

  <ul>
    <% allArticles.forEach(article => { %>
      <li>
        <a href="/article/<%= article._id %>"><%= article.title %></a>
        <p>❤️ <%= article.likes %> إعجاب</p>
      </li>
    <% }) %>
  </ul>

  <div class="nav">
    <a href="/">العودة</a>
    <% if (!user) { %>
      <a href="/auth/google">تسجيل الدخول</a>
    <% } else { %>
      <a href="/logout">تسجيل الخروج</a>
    <% } %>
  </div>

</body>
</html>
