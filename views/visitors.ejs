<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>مرحباً</title>
  <style>
    body {
      background-color: #f0f4f8;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      text-align: center;
      padding: 50px;
      direction: rtl;
    }

    h1 {
      color: #333;
    }

    p {
      font-size: 22px;
      color: #444;
      margin-bottom: 30px;
    }

    .links a {
      display: inline-block;
      margin: 0 10px;
      padding: 12px 20px;
      background-color: #007BFF;
      color: white;
      text-decoration: none;
      border-radius: 8px;
      transition: background-color 0.3s ease;
    }

    .links a:hover {
      background-color: #0056b3;
    }

    .header img {
      width: 30px;
      height: 30px;
    }
  </style>
</head>
<body>
  <div class="header">
    <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="logo">
    Articles
  </div> 

  <% if (user) { %>
    <p>👋 أهلاً <strong><%= user.displayName %></strong>!</p>
  <% } if (userType === "guest") { %>
    <p>👤 مرحبًا أيها الزائر</p>
  <% } %>

  <div class="links">
    <a href="/article/new">✍️ كتابة مقالة</a>
    <a href="/show">📚 عرض المقالات</a>

   <% if (user) { %>
    <a href="/logout">🚪 تسجيل الخروج</a>
  <% } else if (userType === "guest") { %>
    <a href="/logout">🚪 تسجيل الدخول</a>
  <% } %>
    
  </div>

</body>
</html>

